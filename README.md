PDF Editor & Summariser (v3) - frontend updated with navbar & loader

How to run:

1. python -m venv venv
2. venv\Scripts\activate (Windows) or source venv/bin/activate (mac/linux)
3. pip install -r requirements.txt
4. python app.py
5. Open http://localhost:5000 and login (admin / admin123)
